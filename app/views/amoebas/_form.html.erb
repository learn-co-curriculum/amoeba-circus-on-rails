<%= form_for(@amoeba) do |f| %>
  <% if @amoeba.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@amoeba.errors.count, "error") %> prohibited this amoeba from being saved:</h2>

      <ul>
      <% @amoeba.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :generation %><br>
    <%= f.number_field :generation %>
  </div>
  <div class="field">
    <%= f.label :talent_id %><br>
    <%= f.collection_select :talent_id, talents_for_select, :id, :name, {:selected => @amoeba.talent_id} %>
  </div>

  <div class="field">
    <%= f.label :acts, "Select Acts" %><br>
    <%= f.collection_check_boxes(:act_ids, Act.all, :id, :name) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
